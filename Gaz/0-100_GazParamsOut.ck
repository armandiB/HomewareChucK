public class GazParamsOut{
    
    int dimensions[]; //TBC as an array, not good in GazParams, running update several times
    int nbParticules;

    fun pure void init();

    fun pure void update();
}

